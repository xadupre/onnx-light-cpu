.. _processor-performance-gallery:

Processor performance
=====================

This gallery measures the effective cache, memory, and arithmetic performance
visible to ``onnx-light-cpu`` on the current host. Unlike the benchmark gallery,
it characterizes the processor itself rather than comparing kernels or
back-ends.
